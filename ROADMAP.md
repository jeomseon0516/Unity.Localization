# Localization 로드맵

우선순위: `P0` 결함·안전성 → `P1` 핵심 구조 → `P2` API·성능 → `P3` 장기 확장

## 작업 순서

1. **P0-01 — 동기 대기 제거 검토**
   - 초기화 과정의 `WaitForCompletion`이 플랫폼별로 안전한지 확인하고 비동기 경로를 제공합니다.
2. **P1-01 — LocalizeStringEvent와 중복되는 래퍼 정리**
   - 직접 대체 가능한 동작과 패키지가 추가로 제공하는 기능을 분리합니다.
3. **P1-02 — Listener 수명 관리**
   - 반복 설정 시 이벤트가 중복 등록되지 않고 owner 파괴 시 해제되는지 검증합니다.
4. **P2-01 — Locale·Entry 오류 모델**
   - 존재하지 않는 locale/table/entry를 로그가 아닌 명시적인 결과로 반환합니다.
5. **P3-01 — Reactive 연동**
   - Reactive 패키지와 결합하지 않는 선택적 어댑터로 locale 변경 스트림을 제공할지 검토합니다.
